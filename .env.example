## Local environment variables (copy to `.env.local`)
##
## IMPORTANT: never commit real keys to git.
## Only commit this `.env.example` template to the repo.

# Tambo
VITE_TAMBO_API_KEY=

# Optional analytics (see `client/index.html`)
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# Optional OAuth / portal integration (see `client/src/const.ts`)
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=

# Optional maps integration (see `client/src/components/Map.tsx`)
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
