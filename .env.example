# Public browser project key used by the official Tambo React SDK.
# Never put private model-provider or server credentials in a VITE_ variable.
VITE_TAMBO_API_KEY=your_tambo_project_api_key

# Optional Tambo project environment (for example: production).
VITE_TAMBO_ENVIRONMENT=
